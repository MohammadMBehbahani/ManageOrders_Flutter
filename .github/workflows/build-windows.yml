name: Build Flutter Windows EXE

on:
  push:
    branches: [ main ]  # change to your branch if needed

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v3

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.0'

      - name: Enable Windows support
        run: flutter config --enable-windows-desktop

      - name: Get dependencies
        run: flutter pub get

      - name: Build Windows app
        run: flutter build windows --release

      - name: Debug: Show Windows build directory structure
        continue-on-error: true
        run: |
          echo "Listing full build/windows directory:"
          dir build\windows /s

      - name: Upload Windows EXE Artifact
        if: success()  # Only attempt if previous steps didn’t fail
        uses: actions/upload-artifact@v4
        with:
          name: windows-exe
          path: build/windows/runner/Release/
